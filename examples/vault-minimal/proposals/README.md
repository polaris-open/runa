# Proposals

Proposals created by `runa propose` are written here as Markdown files named
`<UTC-timestamp>-<slug>.md`.

A proposal is a suggested change. In v0.1 there is **no apply step**: Runa never
edits your source notes automatically. You read a proposal and decide what to do
with it yourself.

This directory is excluded from `runa scan`.
