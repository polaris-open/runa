"""Runa — Local-first, Markdown-first agent for knowledge bases and project workflows.

Runa is an operator for Markdown knowledge bases and project workflows.
It is NOT a chatbot over notes.

Status: Draft v0.1 — early concept / runnable skeleton.

This v0.1 skeleton does not call external LLMs, does not implement retrieval
(RAG), and does not modify vault files silently. See README.md and docs/ for
the full picture.
"""

__version__ = "0.1.0"
__all__ = ["__version__"]
